const GameCard = ({ pergunta, correctAnswer, wrongAnswers }) => {
  const Buttons = () => {
    const correctButtons = (
      <button>
        { correctAnswer }
      </button>
    )
  }

  return (
    <div>
      
    </div>
  )
}